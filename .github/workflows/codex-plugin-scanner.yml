name: Codex Plugin Scanner CI

on:
  push:
    branches: [main]
    paths:
      - '.codex-plugin/**'
      - 'skills/**'
      - '.mcp.json'
  pull_request:
    branches: [main]
    paths:
      - '.codex-plugin/**'
      - 'skills/**'
      - '.mcp.json'

permissions:
  contents: read

concurrency:
  group: scanner-${{ github.ref }}
  cancel-in-progress: true

jobs:
  scan:
    name: Plugin Scanner
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.2.2

      - name: Run scanner
        uses: hashgraph-online/hol-codex-plugin-scanner-action@3e035f4f2fdd2a2505bb7ace527da3a3e8aef8a6
        with:
          plugin_dir: "."
          mode: scan
          fail_on_severity: critical
