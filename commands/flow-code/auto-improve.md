---
name: flow-code:auto-improve
description: "Run autonomous improvement loops over an existing codebase"
argument-hint: "\"goal\" [--scope dirs] [--max N] [--watch]"
---

# IMPORTANT: This command MUST invoke the skill `flow-code-auto-improve`

The ONLY purpose of this command is to call the `flow-code-auto-improve` skill. You MUST use that skill now.

**User input:** $ARGUMENTS

Choose this front door when the goal is autonomous improvement loops over an existing codebase rather than a single planned feature flow. Use `/flow-code:go` for end-to-end execution, `/flow-code:prime` for readiness assessment, and `/flow-code:simplify` for focused behavior-preserving cleanup.

Pass the user input to the skill. The skill handles everything automatically.
