---
name: flow-code:interview
description: "Refine an epic, task, or spec through a structured interview"
argument-hint: "[epic ID, task ID, or file path]"
---

# IMPORTANT: This command MUST invoke the skill `flow-code-interview`

The ONLY purpose of this command is to call the `flow-code-interview` skill. You MUST use that skill now.

**User input:** $ARGUMENTS

Choose this front door when you want guided refinement of a rough epic, task, or spec before planning. Use `/flow-code:brainstorm` for broader open-ended exploration and `/flow-code:plan` once the scope is ready for concrete task breakdown.

Pass the user input to the skill. The skill handles the interview logic.
