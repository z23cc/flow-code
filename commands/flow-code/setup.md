---
name: flow-code:setup
description: "Set up optional local flowctl tooling and project docs"
argument-hint: ""
---

# IMPORTANT: This command MUST invoke the skill `flow-code-setup`

The ONLY purpose of this command is to call the `flow-code-setup` skill. You MUST use that skill now.

Choose this front door when you want optional local setup for power-user workflows. Use `/flow-code:go` for normal day-to-day execution; the plugin works without running setup.

This is an **optional** setup for power users. Flow-next works without it.
