---
name: flow-code-epic-review
description: Deprecated. Use /flow-code:run instead.
deprecated: true
---
**This skill is deprecated.** Use `/flow-code:run` instead, which manages the full pipeline via `flowctl phase next/done`.

If invoked directly, this skill will redirect to flow-code-run with appropriate arguments.
